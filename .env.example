# IMPORTANT:
# The 'env_file' section loads environment variables INTO the container at runtime.
# These are useful for app configuration inside the container, not for build-time arguments.

NODE_PORT=3010
